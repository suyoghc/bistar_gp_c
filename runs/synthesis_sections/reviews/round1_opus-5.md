# Round-1 review — Opus 5 (fresh in-session subagent, model=opus), repo read access, package + artifacts

REVISE

1. **[S2] [docs/paper-sie-jmp/02-machinery.md:31-35]** "Under `pw_kl_vcal`, the candidate variance is calibrated to the GP pattern, so the parameter-dependent part of the divergence reduces to variance-weighted squared error. Gaussian MLE refits and direct minimization of `pw_kl_vcal` therefore agree in the prior-only mechanism check." — The "therefore" is a non-sequitur. `bistar_gp/metrics_v2.py:43-55` computes `mean(0.5*(mu_q-mu_p)**2 / var_p)` with `var_p = diag(cov_psi)` varying **per evaluation location**, so reduction to *variance-weighted* squared error does not by itself imply agreement with an (unweighted) Gaussian MLE refit. The agreement holds only because the prior-only check gives each draw a single scalar noise variance (`experiments/mechanism_figure_poster.py:264-265, 283` uses scalar `s2psi[i]`), making the weight constant within a draw. As written it invites the general conclusion that projection is metric-independent, which contradicts §2.4's own "GP-uncertainty-weighted" description. Fix: scope to the scalar-variance case; note the projections separate where pattern variance varies across locations.

2. **[S2] [docs/paper-sie-jmp/02-machinery.md:73-75]** "Expected-posterior aggregation first normalizes candidate support within each pattern and then averages, matching Eq. 4 of van Bork, Romeijn, and Wagenmakers." — Unqualified "matching" re-introduces the gap that Case A's review round confirmed and fixed (AO2, S2, CONFIRMED; cf. F-A2). Case A §3.1 row 3 states the load-bearing qualification: their Eq. 4 treats each per-atom quantity as a Bayesian posterior model probability under an infinite-data idealization, whereas the present quantity is a Boltzmann softmax; agreement follows only because both collapse to the same hard nearest-model assignment as τ→0. Fix: scope the match to operation order + the τ-to-zero unique-winner coincidence. Same fix at 08-discussion.md:14-15 ("obtains external correspondence" needs the low-temperature scoping).

3. **[S2] [docs/paper-sie-jmp/08-discussion.md:22-24]** "comparisons across metrics require tau-free draw-win fractions alongside soft probabilities" — implies draw-win fractions license cross-metric comparison. They do not. Draw-win fractions are invariant only under a *common* positive affine rescaling; Case D's actual relation is **candidate-specific** (divisor 743 to 7,161), which can reorder candidates within a draw, so draw-win fractions differ across those metrics too. Case D states the diagnostic supports only "a metric-specific asymmetric-recovery statement." Fix: add the metric-specificity clause.

4. **[S2] [docs/paper-sie-jmp/08-discussion.md:46-49]** "Case D contributes the first correct-specification reference material for this program: known-truth distributions of mean \(G\)…" — omits that the material is in the legacy `pw_nll` metric; under W1 the reference levels calibrate no manuscript-facing metric. Fix: append the legacy-metric scoping and note `pw_kl_vcal` has no correct-specification reference distribution yet.

5. **[S3] [docs/paper-sie-jmp/02-machinery.md:86-99]** Remark 1 writes the containment as \(M_r\subset M_e\) with parameter-space minima, presupposing a common parameter space. The inequality and its aggregation corollary are correct, and all three conventions in §2.3 do preserve the ordering (each is monotone in the per-draw best-instance divergences). But Case B, cited as a worked instance, has cross-family nesting with **different parameter dimensions** and derives the inequality "from reachable-set containment in data space" precisely because parameter-space containment does not apply. Two smaller scope slips ride along: the conventions are stated at parameter level but applied at model level here, and "under any convention" universally quantifies past the three conventions actually covered. Fix: state containment of reachable predictive sets (via the embedding), define model-level scores \(G_i(M)=\min_{\theta\in M}G(\psi_i,\theta)\), and narrow to the three monotone conventions.

6. **[S3] [docs/paper-sie-jmp/02-machinery.md:131-136]** The normative reporting rule justifies draw-win fractions solely by invariance "under a common positive affine rescaling," but the failure mode the preceding paragraph cites is a candidate-specific affine identity, which the invariance does not cover. Fix: add the metric-specificity clause to the rule.

7. **[S3] [docs/paper-sie-jmp/02-machinery.md:79-84]** The D60 Resolution paragraph frames the aggregation trade symmetrically, and 08:14-17 repeats the symmetry; the synthesis nowhere states that the canonical pooled convention outright fails Case A's Target A. Fix: add the qualitative failure clause (no numeral needed); mirror in 01.

8. **[S3] [docs/paper-sie-jmp/01-intro.md:61-63]** Contribution (iii) with the Case A preview invites the reading that the external validation tests contribution (i), the GP data prior; Case A states the checks bypass the GP construction. Fix: scope the validation to the induced-prior and soft-transfer machinery against supplied data priors.

9. **[S3] [docs/paper-sie-jmp/08-discussion.md:111-114]** The uncertainty-floor sentence is uncited and unsupported by any of the four cases; its only home is the section-7 debias development (currently a stub). Fix: cite section 7 / mark program-level.

10. **[S4] [docs/paper-sie-jmp/02-machinery.md:111]** "Following W1, `pw_kl_vcal` provides the primary metric" — incoherent predication. Fix: "W1 fixes `pw_kl_vcal` as the primary metric: …".

11. **[S4] [docs/paper-sie-jmp/08-discussion.md:60-61]** "its importance ratios approximate leave-one-out posteriors, with Pareto smoothing diagnosing unstable ratios" — importance ratios do not approximate posteriors; reweighting posterior draws approximates leave-one-out quantities; Pareto smoothing stabilizes the largest ratios while the shape estimate diagnoses reliability. Fix: correct the mechanics.

12. **[S4] [docs/paper-sie-jmp/02-machinery.md:144]** `[^machinery-metrics]` anchors the metric-roles paragraph on Gneiting and Raftery (2007); variance calibration discards exactly what propriety scores, so the citation invites a propriety claim the section never makes. Fix: re-scope the footnote.

Rationale (abridged): constraint compliance clean; the zero-numerals policy holds exactly (every numeral-bearing line enumerated: identifiers, notation, decision IDs, paths, bibliography only); §8.6's verification claims check out against the repository (four reviewer files per case incl. Kimi K3, author sign-off sections, D62-D65 review outcomes; the D17 provenance-exception attribution matches D65's wording); Remark 1's core mathematics sound (containment correct; all three conventions verified monotone separately); the scale-invariance warning literally correct. REVISE rests on the four S2 items; all twelve are prose fixes compatible with the zero-numerals policy.
