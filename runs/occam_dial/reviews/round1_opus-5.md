# Round-1 review — Opus 5 (claude-opus-5 via Claude Code Agent tool, model=opus), repo read access, package + artifacts
# Channel: in-session subagent per HANDOFF §4 reviewer table. 30 tool uses, ~51 min.

REVISE

1. **[S2] [docs/paper-sie-jmp/04-case-B-occam-dial.md:73; runs/occam_dial/README.md:32]** "Sinusoidal overtakes at τ=1.484355, bracketed by 1.412538 and 1.496236", with the README asserting that "the bracket ... provides the resolution statement" — the grid bracket is not the binding uncertainty for this pair. At grid indices 66–67 the run's own ESS is 883/900 (Sin+Linear) and 1454/1521 (Sinusoidal), so the IS standard error on log Z_M(enc) − log Z_M(res) is ≈ sqrt(1/883 + 1/1454) = 0.043 nats, while the entire delta swing across the reported bracket is 0.040 nats (+0.03435 to −0.00552). A ±1 SE rigid shift of the delta curve (the draws are shared across τ, so the error is common-mode) moves the crossing by ≈ ±1 grid step, τ ≈ 1.39–1.58. The stated bracket therefore understates the uncertainty by roughly 2x, and six decimals on the interpolant are meaningless. Fix: report the Sinusoidal crossing as τ ≈ 1.5 with an ESS-derived interval (or raise n_is on the Sin+Linear sweep until SE(delta) is well below the bracket swing), and change README:32 to state that resolution is set by the larger of grid spacing and the ESS-implied MC error. The Linear crossing needs no change (SE 0.012 vs swing 0.354). Propagate the same correction to the `kb/Wiki/REVIEW_AND_VET.md` resolution paragraph, which repeats the number.

2. **[S2] [docs/paper-sie-jmp/04-case-B-occam-dial.md:53,63-66]** "E6 instead tests the two exact restrictions in data space" / "Both numerical inequalities therefore hold by margins far above the 10^-8 comparison tolerance. This result supports the reachable-set claim" — the inequality cannot fail as constructed, so it supplies no empirical support. `_exact_encompassing_space` (experiments/e6_nesting_monotonicity.py:67-80) makes each restricted box an exact subset (Linear a∈[-2,2], b∈[-5,5] maps onto b,c at A=0; Sinusoidal A∈[0.01,5]⊂[0,5], same ω, φ, at b=c=0), Ḡ depends only on the predicted mean, and the encompassing multi-start is seeded with the exact embedded restricted optima (e6:191-192) which L-BFGS-B can only improve on. The script's own comment (e6:230) says the embeddings "guarantee the optimization inequality". min_φ Ḡ(M_e) ≤ min_φ Ḡ(M_r) is thus an analytic consequence of the boxes the script itself defines, and the 10^-8 tolerance can never arbitrate a violation — contradicting the work order's premise that a violation was a possible reportable outcome. Fix: rewrite 63-66 to say E6 confirms the machinery reproduces the analytic consequence of the box containments and quantifies the margins (2.379/2.501 nats), and place the section's empirical weight on the finite-τ crossings; also fix line 46, where the inequality follows *from* the reachable-set containment rather than being "required" by it.

3. **[S3] [docs/paper-sie-jmp/04-case-B-occam-dial.md:19,71-73]** Figure 4's τ=0.3 is presented as a neutral evaluation point, but §4.2 reports the occam=True Linear/Sin+Linear crossing at τ=0.295184 — 1.6% below it. The occam=True panels therefore sit essentially at the tie: p2's log Z gap is 0.0867 nats (Bayes factor 1.09), and at any τ below 0.295 the encompassing model wins even under occam=True. The section never connects the two facts, leaving the p1/p2 "Linear preferred" rung looking more robust than it is. Fix: add one cross-reference sentence in §4.1 noting τ=0.3 is just above the occam=True crossing, so the p1/p2 ordering is τ-marginal and the robust content is the p2-to-p3 magnitude change.

4. **[S3] [docs/paper-sie-jmp/04-case-B-occam-dial.md:30-31; runs/occam_dial/README.md:16]** Six-decimal model probabilities (0.534121 … 0.991758) are reported with no Monte Carlo error statement, and the anchor tolerance is justified only by three-decimal rounding plus "cross-platform optimizer differences". From the arm ESS recorded in the same JSON (Linear 11363, Sin+Linear 3180, Sinusoidal 681 at n_is=40k), SE(log Z) ≈ 0.009/0.018/0.038 nats, giving SE ≈ 0.020 nats on the p2 Linear−Sin+Linear log gap and ≈ 0.005 on each probability — larger than the 0.003 anchor tolerance and swamping the last three printed digits. Fix: quote three decimals in prose and README, add one sentence with the ESS-implied uncertainty, and describe 0.003 as a same-seed reproduction gate rather than an accuracy claim.

5. **[S3] [experiments/occam_dial_figure.py:103; runs/occam_dial/README.md:18]** The JSON key `model_posterior` and the README heading "Fresh n=50 posteriors" mislabel the plotted quantity. No likelihood enters: the value is softmax over log Z_M, i.e. the normalized induced model *prior*. The upstream producer returns it as `priors` (bistar_viz/scripts/_viz_spaces.py:220,251), the figure's own y-axis reads "Induced model prior", and the section explicitly disclaims posterior inference (lines 21-22). The artifact naming invites exactly the misreading W4 exists to prevent. Fix: rename the key to `model_prior` and the README heading to "Fresh n=50 induced model priors"; regenerate.

6. **[S3] [experiments/occam_dial_figure.py:106-111; runs/occam_dial/figure_results.json arms.p1_priors_lap_occam.ess]** The pure-Laplace arm records `ess: null` and nothing else, because `model_prior_curves` discards `ZMxResult.n_clipped`/`converged`. bistar_gp/laplace_evidence.py:213-228 documents that a clipped Hessian eigenvalue injects an arbitrary floor-dependent term (~+9.2 nats per direction) and instructs that any `n_clipped > 0` value be treated as floor-dependent. The committed artifact therefore cannot show whether p1's numbers carry that term — and p1 is precisely the rung whose estimator error the ladder attributes. Fix: call `laplace_log_Z_Mx` directly for the p1 arm (same package function, no API change) and record `n_clipped`/`converged` per model, or state in the README that the run emitted no Hessian-regularization warnings.

7. **[S4] [runs/occam_dial/figure_results.json:119-120; runs/occam_dial/README.md:24]** `"optional_local_crosscheck": {"available": true}` and "The optional D17 table cross-check was available." bake the presence of the untracked local `runs/viz_unification/delta_table.md` into committed artifacts, so a clean-clone rerun regenerates a different JSON block and a different README line even though every computed number matches. Fix: mark the block machine-dependent in the README, or emit crosscheck prose that does not vary with local state.

8. **[S4] [experiments/e6_nesting_monotonicity.py:110-113]** `_crossings` records a crossing whenever `left == 0.0`, regardless of `right`'s sign, and labels `winner_below` "restricted" at a delta of exactly zero; an exact zero at an interior grid point is also recorded twice (once via the previous index's `left*right > 0` fall-through, once via the `left == 0.0` branch). Not exercised by this data — no delta is exactly zero, so no reported number changes. Fix: `continue` when both endpoints are zero, and skip the `left == 0.0` branch when the previous index already emitted that bracket.

**Rationale.** Constraint compliance is clean: no arrow glyphs, no "X is the Y", no lives/sits, em-dashes only in the mandated evidence-tier footnote format; no Mauna, no withdrawn HMC cache, no `toy_tau_metric_comparison`, no `kl_forward`; `pw_kl_vcal` confirmed as the package default actually used; no `bistar_gp/` change; commit scope and the 155 KB figure are within limits; D62 is same-commit and non-colliding; the `kb/Wiki/REVIEW_AND_VET.md` resolution exists on disk with numbers matching the JSON (absent from the diff only because `.gitignore:36` excludes `kb/`, so the package NOTE's expectation there is mistaken, not a defect). Every quoted number reconciles with the artifacts, and several independent cross-checks pass: p2 minus p3 log Z equals each model's `_log_reference_volume` to ~1e-15, the volumes match the declared boxes exactly (including the 0.002-nat A∈[0,5] extension), the τ grid and both interpolated crossings reproduce from the recorded brackets, and the D17 legacy provenance (0.934/0.693, `a87356a`, occam hard-ON in the priors script) checks out against D17 and `viz_unification_compare.py`. The two S2 items are about claim strength rather than arithmetic: the Sinusoidal crossing is reported at a precision the run's own ESS cannot support, and the min-Ḡ inequality is guaranteed by the construction that tests it, so it cannot bear the evidential weight §4.2 assigns it. Both are fixable by rewriting prose and one optional rerun; no scope expansion is implied.
