# Round-1 review — Kimi K3 (package-only, via OpenRouter)

VERDICT: APPROVE

Findings:

1. **S4 — runs/toy_debias_demo/README.md, "Scope note" paragraph, line "thesis ch. 5 is the source of the program."** This is the "X is the Y" role-noun construction barred by the style constraints (§0: no "X is the Y" role-noun constructions). The same content is already expressed compliantly in section 07 ("The program originates in thesis chapter 5"). Fix: reword to "the program originates in thesis ch. 5" (or similar verb construction). Note the README is regenerated by `write_readme()` in `experiments/toy_debias_demo.py`, so the fix must be made in the script's `lines` list, not just the committed file, or the next rerun reintroduces it.

2. **S4 — docs/paper-sie-jmp/07-debias-bridge.md §7.1 and runs/toy_debias_demo/README.md "Diagnostics".** `results.json` records `acceptance_rate_by_chain: [0.992, 0.998]`, far above `target_accept_prob: 0.8`; neither the prose nor the README reports this. Not statistically alarming (adaptation settled on a conservative step size; final step sizes 0.47/0.42 are recorded, divergences zero, ESS healthy), but a reviewer checking diagnostics against the artifact will find an unreported number that superficially contradicts the stated target. Fix: add one clause to the README Diagnostics list, e.g. "mean acceptance by chain 0.992 and 0.998 against target 0.8, indicating conservative adapted step sizes," and optionally mirror in prose.

Numbers audit (all verified against `results.json` at tip 8f1326d):

- Slope posterior 0.197 / 0.072 / [0.033, 0.323], contains 0.250: matches (0.19750, 0.07214, [0.03276, 0.32299]) ✓. The SD correctly uses the law of total variance over the draw mixture (`total_variance_sd`), and the interval is the exact mixture central interval by bisection — estimator conventions are internally consistent between script, JSON, README, D67, and prose ✓.
- RMSE 1.430 / 0.403 / 1.028 / 71.9%: matches (1.43039, 0.40252, 1.02787, 71.8596) ✓; per-chain 1.431/1.430 and 0.403/0.402 ✓. Percentage correctly taken relative to composite RMSE ✓.
- Scale references: drift RMS 1.451, sin RMS 0.690 ✓; the "composite reproduces the drift" reading (1.430 vs 1.451) is sound.
- Coverage 174/201 = 0.8657 → reported 0.866 ✓, and the prose correctly frames it as a pointwise summary on a correlated grid rather than a calibration claim — this is the right level of epistemic caution.
- Band widths 1.836 vs 1.032 ✓; diagnostics 0 divergences, R-hat ≤ 1.0025, bulk ESS ≥ 602.4, tail ESS ≥ 502.6 ✓. The same-MAP-init R-hat limitation is disclosed in prose, README, JSON, and D67 — the required four-place disclosure is complete.
- Structure-check numbers (4.456e-13, 1.896e-15) in D67 match JSON ✓; the slope-read derivation (endpoint secant for E[b], rank-one variance at |x| ≥ 1) is mathematically correct for gpytorch's `LinearKernel`.

On the scrutinized design choice: rejecting `bistar_gp.debias.decompose_model_hmc` because it drops per-draw conditional covariance (debias.py:206) is correct — using it would indeed understate debiased uncertainty and corrupt the coverage number, and the documented reason is the right one. The alternative (analytic within-draw marginalization via `decompose_additive_gp`, which for an additive kernel returns exactly the marginal of the joint conditional Gaussian, plus Monte Carlo over draws with exact mixture quantiles) is the statistically correct construction, and using all 1,000 draws removes an unnecessary RNG dependence. The within-draw claim in §7.1 ("the component posterior ... already forms the marginal of the joint conditional Gaussian") is correct for the additive decomposition.

On the known open item: I see no sharper inconsistency than the known one. Section 08's footnote wording ("supplies no reported number") is now stale given §7's reported numbers, exactly as flagged in the driver facts — an assembly-time fix, not re-raised as a new finding.

No S1/S2/S3 defects found. The package is statistically coherent, every reported number regenerates from the artifact, the undercoverage result is reported as it came out rather than spun, and the kernel-labeling caveat is stated with appropriate force.