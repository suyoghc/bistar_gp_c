# Adversarial cross-check — Kimi K3 (checks 1-4 on Opus singles F2/F3/F5/F6)

CHECK-1: REFUTED. The coverage sentence in §7.1 makes no comparative claim; it states an absolute fact (0.866 against nominal 0.95) with an explicit and correctly worded caveat that the grid is correlated and this is not a calibration test. The omitted composite coverage (0.821) would make the debiased arm look *better*, so nothing reported is misleading or reversed by the omission — the text's self-assessment is, if anything, more pessimistic than the full comparison warrants. Reporting all three coverages is a legitimate enrichment, but the absence of a comparator the prose never invokes is not a one-sided-reporting defect at S2.

CHECK-2: CONFIRMED. The clause "additional observations sharpen the composite while leaving the attribution comparatively uncertain, and honest inference retains an uncertainty floor that sample size does not remove" is an N-asymptotic assertion sitting inside a results paragraph whose entire evidence base is one fixed N=20 run. Worse, it is theoretically contestable here: the bias family is a one-parameter linear component under a proper prior, and with an additive SE+linear fit the slope attribution can in fact sharpen with N, so "sample size does not remove" the floor is not entailed by the demonstrated setup. The 8.5 framing licenses stating this as a program-level *expectation*, and the width ratio (1.836 vs 1.032) is a fine concrete illustration, but the sentence as written asserts the asymptotic behavior rather than attributing it. The proposed fix — restrict the sentence to the measured N=20 statement or add a small N-sweep — is correct and cheap; severity S2 is fair.

CHECK-3: REFUTED. "It removes most of it" is true under every candidate statistic: 71.9% RMSE reduction, 1.028 removed against a drift RMS of 1.451 (~71% of displacement magnitude), or the 79% direct figure. The paragraph already supplies the direct scale references (composite 1.430 vs drift RMS 1.451; residual 0.403 vs sin RMS 0.690), so the reader is given both framings and the sentence is explicitly anchored to the RMSE numbers just stated. Since the composite error essentially *is* the drift (the text's own point), RMSE-against-sin(x) reduction is a legitimate proxy for displacement removed, not a wrong statistic. The proposed rewrite substitutes one valid number for another valid number already in the artifact; that is preference, not a defect.

CHECK-4: REFUTED (with a cosmetic residue). The latent-band convention is not hidden: §7.1 states explicitly that "all bands are latent-function bands with no observation noise added," the README and results.json repeat it, and it matches the repo's established `honest_band_decomposition.py` convention. Given that disclosure, "posterior predictive" used loosely for the latent composite is a terminology looseness, not a methods-facing error, and observations falling outside a latent band is expected behavior the text never denies. "Describes well" is soft, but the figure itself shows the fit and the composite coverage number the finding demands is a number the same reviewer (CHECK-1) argued was wrongly omitted from the section — the two fixes pull in opposite directions, which undercuts both being mandatory. At most this warrants a one-word caption tweak ("latent posterior predictive") at assembly; it does not rise to a revision-blocking defect.