# Round-1 review — Kimi K3 (moonshotai/kimi-k3 via OpenRouter, author-directed; package-only, no repo access)
# Channel note: run 2026-08-12 on the ORIGINAL round-1 package (branch tip a4b92c0 state), AFTER the fix passes had landed; convergences with already-resolved findings are noted in VERDICTS. Temperature 0.3, reasoning budget 24576, completion 49152; first attempt (16384) exhausted on reasoning and was retried.

VERDICT: APPROVE

Findings:

1. [S3] [docs/paper-sie-jmp/06-case-D-mopen-calibration.md, winner-labels table, "all configurations agree" row] The column headers read "`pw_hellinger`, Power / Exponential" etc., but the final row's cells (39 / 50, 49 / 50, 48 / 50) are agree/total counts from `robustness_across_configs`, not Power/Exponential winner splits — a reader scanning the column will misparse 39/50 as a winner split. The numbers themselves match the JSON (agree 39, 49, 48). Fix: restructure the row to span columns (e.g., "all configurations agree: 39/50, 49/50, 48/50 by metric") or add a footnote clarifying the row's cells are agreement counts, not winner splits.

2. [S4] [docs/paper-sie-jmp/06-case-D-mopen-calibration.md, "What the stored comparisons declined"] The claim that `pw_nll` "combines mean mismatch with pointwise variance calibration" is a definitional claim about the stored metric that no artifact in this package defines (the JSON only carries the metric name and values). I cannot verify it from the package. Fix: either cite the artifact/source that defines `pw_nll`, or soften to "the closest available stored quantity to the W1 primary role" without the compositional rationale.

3. [S4] [docs/paper-sie-jmp/06-case-D-mopen-calibration.md, footnotes] The three footnote badge lines use em-dashes ("🟢 peer-reviewed — Navarro…", "🟠 empirical — …"). The style rule calls for minimal em-dashes; if this badge-plus-dash format is the house citation convention used elsewhere in docs/paper-sie-jmp/, it is acceptable, otherwise replace with a colon or parenthesis. Flagging for consistency check only.

Verification notes (no findings): all section numbers I could check trace exactly to results.json — BIC 18/32 and 41/50; the full 3x3 winner-label table; pw_nll accuracies 35/37/37; median max-probabilities 0.522/0.522/0.518 (0.522309, 0.521803, 0.517878); all twelve mean_G cohort means; subject-25 example (4.881/4.852, diff 0.029); regret means 21.383/20.681 and 35.587/14.855; peaks 33.782 and 91.452 at trial 1; gap shares 70.0/82.2 and 41.7/77.7; trial-1 regrets 116.333/82.551 and 131.415/39.963; the 4.582–4.858 correct-specification range. Constraint compliance holds: no withdrawn M2bR caches cited, no viz_unification numbers, no Mauna Loa, no pw_kl_vcal relabeling, no bistar_gp changes, absolute-difference formula per the binding work order, E8B deferral labeled and recorded in D64, PNG 159,881 bytes < 2 MB, D64 entry present, no arrow glyphs or banned role-noun constructions detected. The practitioner-MAP conditioning limitation and the squared-vs-absolute discrepancy are disclosed in the section, README, JSON provenance, and D64. The script's latent `posterior_sd_min/max_raw` fields (sqrt of a possibly slightly negative eigenvalue) are dead — never serialized — so the NaN risk does not affect any artifact; not a finding given the verified clean run, but worth deleting in a future touch-up.