# Presence of this file at the repo root puts the root on sys.path (pytest
# "prepend" import mode), so `import bistar_gp` works without installing the
# package. Remove once the project ships a pyproject.toml + `pip install -e .`.
